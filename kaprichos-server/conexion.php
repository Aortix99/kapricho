<?php
$host = "localhost";
$username = "root";
$password = "";
$database = "kapricho";

$conexion = mysqli_connect($host, $username, $password, $database);

/* if($conexion){
    echo "conexión exitosa";
} else {
    echo "error";
} */

?>